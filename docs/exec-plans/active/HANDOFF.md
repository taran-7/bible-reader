# Handoff: де ми зараз

> Для AI-агента: прочитай цей файл першим, коротко перекажи користувачу стан і скажи наступний крок. Після кожного завершеного кроку онови цей файл.

Оновлено: 2026-09-25. Прогрес 27/27; ручну перевірку 7.6 винесено в tech debt #7.

## Стан
- Гілка: `feature/01-bible-reader-mvp` (запушена, PR ще немає).
- OpenSpec change: `bible-reader-mvp`, прогрес 27/27 (`openspec instructions apply --change bible-reader-mvp --json`).
- `swift test`: 57 тестів зелені. Рецензія diff (8.2) зроблена: виправлено текст «Нічого не знайдено» (показує відправлений запит) і захищено `convert_synodal.py` від зсуву номерів віршів. `BibleCore` (імпорт, репозиторій, посилання, пошук, `ReaderViewModel`) готовий.
- Додаток `BibleReaderApp` збирається (`xcodebuild -project BibleReaderApp/BibleReader.xcodeproj -scheme BibleReader build`), UI перевірено вручну, крім помилки бази (7.6).
- Xcode 27.0 встановлено; XcodeGen встановлено (`brew install xcodegen`).

## Зараз: ручна перевірка UI (робить користувач)
Відкрити `BibleReaderApp/BibleReader.xcodeproj` у Xcode, ⌘R. Відмічати, що працює:

**7.3 Навігація**
- [x] Бічна панель: книги в секціях «Старий Заповіт» / «Новий Заповіт».
- [x] Genesis 50 → ▶ відкриває Exodus 1.
- [x] На Genesis 1 кнопка ◀ неактивна.
- [x] Вибір розділу в тулбарі працює.
- [x] Перемикач KJV → Синодальний: та сама книга й розділ, назви книг російською.

**7.4 Пошук**
- [x] `love` у KJV: є результати, слово підсвічене.
- [x] `любовь` у Синодальному: є результати.
- [x] `любовь` у KJV: «Нічого не знайдено».
- [x] `Ин 3:16`: відкривається Иоанна 3, вірш 16 виділено і видно.
- [x] Клік по результату відкриває цей вірш (виправлено й перевірено).

**7.5 Копіювання**
- [x] Виділити Ин 3:16–18 (Shift-клік), ⌘C → у буфері `«…» (От Иоанна 3:16-18)` (повна назва книги; змінено після перевірки, глянути ще раз).
- [x] Те саме через правий клік → «Копіювати».

**7.6 Помилка бази** (користувач поки пропустив; задача 7.6 не відмічена)
- [ ] У зібраному `.app` (`Show in Finder` → Show Package Contents → Contents/Resources) перейменувати `bible.sqlite`, запустити `.app`: екран «Не вдалося відкрити базу», без падіння.

## Що далі (для агента)
1. PR `feature/01-bible-reader-mvp` → `main` (гілка запушена), коли користувач скаже.
2. PRD продукту: [docs/product-specs/prd.md](../../product-specs/prd.md) (чернетка). Обговорити з користувачем пріоритети й відкриті питання, потім перший пункт роадмапу (v1.1) оформити через `/opsx:propose`.
3. Після мержу: `/opsx:archive`, план `docs/exec-plans/active/bible-reader-mvp.md` перенести в `completed/`.
4. Далі за планом: крок 8 (харнес: `.claude/agents/reviewer.md`, `make check`) і крок 9 (README сабмішену, відео робить користувач).

## Відкрите питання
- У полі пошуку після запиту немає блимаючого курсору. Імовірно, текст виділено повністю (так macOS показує виділення); уточнити в користувача.

## Корисне знати
- База `bible.sqlite` не комітиться; генерується `make db` або pre-build скриптом Xcode.
- `.xcodeproj` генерується з `BibleReaderApp/project.yml` (`cd BibleReaderApp && xcodegen generate`), руками не правити.
- Відомі обмеження: `docs/exec-plans/tech-debt-tracker.md`.
- Користувач спілкується українською.
